package {{pkg}}

import "redhare/game/module/cfg"
{% for i in imports %}
import {{i}};
{% endfor %}

type {{name}} struct {
    cfg.BaseCfg
    {% for f in fields %}
    {{f.field_name}}    {{f.field_type}} // {{f.field_comment}}
    {% endfor %}
}